pub struct InternetAddress(String);

pub struct PublicKey(String);


/// # GatewayAddress
pub struct GatewayAddress(String);

/// # GatewayChainLocation
/// 
/// The GatewayChainLocation is the BorneoAddress of the chosen lattice.
pub struct GatewayChainLocation(String);
