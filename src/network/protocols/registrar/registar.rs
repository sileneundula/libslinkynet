/// # RegistarAuthority
/// 
/// The **RegistarAuthority** for registration.
/// 
/// RegistarAuthorities must use intensive proof-of-work to gain.
pub struct RegistarAuthority();

pub struct RegistarAuthorityCredentials();

pub struct Registar {
    id: u32,
    unique_name: String,
}